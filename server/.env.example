PORT=3000
JWT_SECRET=troque-este-segredo-em-producao
JWT_EXPIRES_IN=12h
# Banco: DATABASE_URL tem prioridade sobre DB_PATH (ambos apontam para o mesmo arquivo)
DATABASE_URL=./data/barbearia.db
DB_PATH=./data/barbearia.db
ADMIN_PASSWORD=admin123
# Gerado automaticamente por: npm --prefix server run seed
# ADMIN_PASSWORD_HASH=$2a$10$...
